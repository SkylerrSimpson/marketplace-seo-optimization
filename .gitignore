# Dependencies
/vendor/

# Secrets
.env

# Logs / sessions
*.log
/tmp/php_sessions/

# OS / editor cruft
.DS_Store
*~

# Note: generated data under */data/ (exports, drafts, output) IS committed so the
# whole team can review results without re-pulling from each marketplace.

# Claude Code local (per-developer) settings
.claude/settings.local.json

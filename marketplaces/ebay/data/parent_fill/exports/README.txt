Drop Usurper child exports here, named exactly:
  dows_pass1.csv ... dows_pass6.csv   (search list = parent_skus_dows_batch1.txt)
  ige_pass1.csv  ... ige_pass5.csv    (search list = parent_skus_ige_batch1.txt)

For EVERY pass this round set Usurper's search column to: parent.sku
(so the export returns each parent's CHILDREN, with their own aspect values).
Columns per pass are listed in ../PASS_COLUMNS.md  (25 cols each).

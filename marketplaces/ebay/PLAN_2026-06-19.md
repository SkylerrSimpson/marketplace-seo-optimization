# eBay Item-Specifics — Plan for 2026-06-19

Status at start of day: Usurper + rule + deep-LLM fill **exhausted for both accounts** (dry-run, no eBay writes).
- DOWS: 4,350 / 8,006 gaps filled (54%) · 3,656 blank (~3,149 correctly un-fillable, 507 hand-fill)
- IGE: 1,407 / 2,538 gaps filled (55%) · 1,131 blank (~1,033 correctly un-fillable, 98 hand-fill)

Two bottlenecks remain: **(A) human review** of proposed values, **(B) prod write creds/scope + Scott sign-off** for write-back.

Today = run both tracks in parallel.

---

## AM — Review enablement (unblock the inventory team first)

Goal: hand the inventory guy focused queues + a guide so he reviews all day in parallel.

1. **`split_review_queues.php`** (new) — from `review_sheet.csv` emit per account:
   - `hand_fill_queue.csv` — `source=none` ∩ triage `candidate_export`, grouped by `category_id` (507 DOWS / 98 IGE). Similar products batched together.
   - `llm_spotcheck_queue.csv` — `source=llm`, sorted by `certainty` ASC (riskiest first; ≥80 can be rubber-stamped).
   - Keep `approved_value` / `reviewer_notes` columns for his input.
2. **Reviewer guide** (`ebay/docs/review-guide.md`) — column meanings, how to fill `approved_value`, when to leave blank, the certainty rule of thumb.
3. Hand off → review runs in parallel the rest of the day.

## PM — Engineering (build dry-run, ready to fire on creds)

4. **`build_apply_set.php`** (new, dry-run) — final value per (listing, aspect):
   - `approved_value` if reviewer filled it, else
   - `proposed_value` when `source ∈ {usurper, rule, default}` (always trusted), else
   - `proposed_value` when `source = llm` AND `certainty ≥ threshold` (configurable, default 80).
   - Output: per-listing final aspect set + a summary of what would be written.
5. **Write-back spike** — settle the transport:
   - Trading `ReviseItem` is **edge-blocked from this network** (documented). Legacy listings aren't in the Inventory API. → investigate **Sell Feed / LMS** revise path; nail the feed type for item-specifics revision.
   - **Merge guard (critical):** eBay replaces the WHOLE ItemSpecifics set → merge current aspects (`items/{id}.json`) + new fills before writing, or already-filled required aspects get wiped.
   - Build `write_back.php` dry-run with `--apply` gated, idempotent, backoff.

## Canary (first real write when creds land)

6. **4 missing-REQUIRED** aspects — 3 DOWS need MPN (cats 183175/163824/116656), 1 IGE needs Type (cat 46413). Smallest, highest-value, safest first write.

---

## Gated / user actions for today
- Walk the inventory guy through `review_sheet.csv` + the new queues.
- Get prod write creds/scope status from Scott (+ sign-off for the canary).

## Hold the line
- NO live eBay writes until creds + sign-off. Everything above is dry-run until then.
